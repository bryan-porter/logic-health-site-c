# Environment Variables for LogicHM Marketing Website

# NEXT_PUBLIC_SITE_URL
# Controls SEO indexing behavior and canonical URLs
#
# PRODUCTION: MUST be set to https://logichm.com
# - Allows search engine indexing (no X-Robots-Tag noindex header)
# - Sets correct canonical URLs for blog posts
#
# PREVIEW/DEV: Can be unset or set to preview URL
# - Adds X-Robots-Tag: noindex, nofollow (prevents search indexing)
# - Canonical URLs default to https://logichm.com if unset
#
NEXT_PUBLIC_SITE_URL=https://logichm.com
